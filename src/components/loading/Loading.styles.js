import styled from 'styled-components'

const LoaderImage = styled.img`
  width: 60px;
  height: 60px;
  position: absolute;
  top: 50%;
  left: 50%;
  margin: -30px 0 0 -30px;
  user-select: none;
`

export { LoaderImage }
